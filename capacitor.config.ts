import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.animaltextbattle.app',
  appName: 'Animal Text Battle',
  webDir: 'out'
};

export default config;
